#include<stdio.h>
int main(){
    int a,b,c;
    printf("enter 3 coefficient: ");
    scanf("%d%d%d", &a, &b, &c);
    printf("%d%d%d", a, b, c);
    float x;
    a * x^2 + b * x + c ==0;
    return 0;
}